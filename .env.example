# Copy this file to ".env" and fill in your values. The .env file is gitignored.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

# review = fill then ask before submitting (safest); auto = fill and submit
SUBMIT_MODE=review

# Show the browser window (false) or run hidden (true)
HEADLESS=false
